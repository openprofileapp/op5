




-- If scheduled, check database for schedule events

-- Change Log
id TEXT PRIMARY KEY NOT NULL,
asset TEXT PRIMARY KEY NOT NULL,
displayName TEXT,
stage TEXT NOT NULL DEFAULT 'incomplete'
    CHECK (visibility IN ('incomplete', 'progress', 'complete')),
version TEXT DEFAULT '0.0.1',
completion INTEGER NOT NULL DEFAULT 0
    CHECK (completion BETWEEN 0 AND 100),

    -- invited accounts with the read permission will bypass schedule